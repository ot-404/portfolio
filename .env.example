# Copy this file to ".env" and fill in real values for local development.
# .env is git-ignored — never commit real credentials.
# On Render, set these in the dashboard (Environment tab) instead.

# Used to sign the session cookie (flash messages). Use any long random string.
SECRET_KEY=change-me-to-something-random

# --- Contact-form email (SMTP) ---
# Gmail example. SMTP_PASSWORD must be a Google "App Password" (16 chars),
# NOT your normal account password. See README for how to create one.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=opshokare@gmail.com
SMTP_PASSWORD=your-16-char-app-password
# Where contact submissions are delivered (defaults to PROFILE email if unset).
CONTACT_TO=opshokare@gmail.com
# Set to "false" only for a local non-TLS test mail server.
SMTP_USE_TLS=true
